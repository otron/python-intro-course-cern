""" Modules!
Python looks for modules in the directories listed in `sys.path`.
`sys.path` includes path listed in the env. variable PYTHONPATH at the time
  when the interpreter is started.
"""


""" Testing!
`unittest` is the stdlib unit testing module.
"TDD is this curious idea that you should write test code before writing any code."


"""
